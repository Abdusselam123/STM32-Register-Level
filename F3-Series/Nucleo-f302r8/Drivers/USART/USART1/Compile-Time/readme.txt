+ This driver is engineered using the compile-time configuration principle. Instead of increasing the CPU load during execution (runtime), all hardware parameters are determined while the code is being compiled. 

+ The user only needs to configure the variables in the user settings section within the .h file according to their needs.